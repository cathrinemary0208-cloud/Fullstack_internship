//code io practise -------------

const data = require("./data")
console.log(data.Name) /// to extract a single value
console.log(data)  
//To only import the required data 
//available in the same name
const {year} = require("./data")
console.log(year)


